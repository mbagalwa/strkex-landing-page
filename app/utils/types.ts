export type T_ABOUT_CARD = {
  id: number;
  title: string;
  badge: string;
  image: string;
  description?: string;
  caracteristics?: string[];
};

export type T_SOLUTION = {
  id: number;
  title: string;
  badge: string;
  image: string;
  description?: string;
  size?: string;
};
